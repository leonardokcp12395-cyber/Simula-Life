# --- NEAT Configuration File ---
# Based on the official documentation for neat-python

[NEAT]
fitness_criterion     = max
fitness_threshold     = 20000
pop_size              = 40
reset_on_extinction   = True

[DefaultGenome]
# --- Core Genome Configuration ---
num_inputs              = 16
num_outputs             = 2
num_hidden              = 0
feed_forward            = True

# --- Activation Function Configuration ---
activation_default      = tanh
activation_options      = tanh
activation_mutate_rate  = 0.1

# --- Node Aggregation Configuration ---
aggregation_default     = sum
aggregation_options     = sum
aggregation_mutate_rate = 0.0

# --- Node Bias Configuration ---
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_init_type          = gaussian
bias_max_value          = 5.0
bias_min_value          = -5.0
bias_mutate_power       = 0.5
bias_mutate_rate        = 0.7
bias_replace_rate       = 0.1

# --- Genome Compatibility Configuration ---
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient = 0.5

# --- Connection Configuration ---
conn_add_prob           = 0.5
conn_delete_prob        = 0.3
enabled_default         = True
enabled_mutate_rate     = 0.05

# --- Initial Connection Type ---
initial_connection      = full_nodirect

# --- Node Addition/Deletion Configuration ---
node_add_prob           = 0.2
node_delete_prob        = 0.1

# --- Node Response Configuration (less critical for feed-forward) ---
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_init_type      = gaussian
response_max_value      = 10.0
response_min_value      = -10.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# --- Connection Weight Configuration ---
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_init_type        = gaussian
weight_max_value        = 8.0
weight_min_value        = -8.0
weight_mutate_power     = 0.5
weight_mutate_rate      = 0.8
weight_replace_rate     = 0.1

[DefaultSpeciesSet]
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 25
species_elitism      = 2

[DefaultReproduction]
elitism              = 2
survival_threshold   = 0.25
min_species_size     = 2
